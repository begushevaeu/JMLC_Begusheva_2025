# PostgreSQL Credentials for Docker
POSTGRES_USER=postgres
POSTGRES_PASSWORD=8495
POSTGRES_DB=aml_project

# Database URL for the API service inside Docker
# Note: it points to the 'db' service, not 'localhost'
DATABASE_URL=postgresql+psycopg2://postgres:8495@db:5432/aml_project
